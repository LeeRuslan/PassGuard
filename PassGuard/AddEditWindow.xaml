<Window x:Class="PassGuard.AddEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit Password" 
        Height="350" 
        Width="450"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Title:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="TitleTextBox" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" Text="Username:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="UsernameTextBox" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="4" Text="Password:" Margin="0,0,0,5"/>

        <Grid Grid.Row="5" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <PasswordBox Grid.Column="0" x:Name="PasswordBox"/>
            <Button Grid.Column="1" Content="Generate" Width="100" Margin="10,0,0,0" Click="GenerateButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>